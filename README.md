## Requirements

* **Python 3.9–3.12**
* **uv** (for managing Python packages)
* **OpenCV**
* **MediaPipe**
* **python-osc**
* **NumPy**

Install using uv:

```bash
uv pip install opencv-python mediapipe python-osc numpy
```

